# ListenAndShowAndTranslate（听·显·译）项目计划书

> 版本：v0.1（立项稿）
> 日期：2026-02-21
> 工作目录：`L:\AI_AudioAndChat\ListenAndShowAndTranslate`
> 一句话定位：**只监听你指定的那一个程序的音频，把它实时变成字幕（原文 / 译文 / 双语），以透明置顶悬浮窗盖在全屏游戏上。**

---

## 0. 场景定义（先锁死需求边界）

用户的真实使用场景是：

```
┌─────────────────────┐        ┌──────────────────────┐
│  小说软件（听书）    │        │  游戏（全屏）         │
│  → 独占音箱里的一路  │        │  → 自己出声音         │
└─────────┬───────────┘        └──────────┬───────────┘
          │ 只有这一路音频被采集            │ 声音照常播放，不进识别
          ▼                                ▼
   ┌──────────────────────────────────────────────┐
   │   本程序：抓音频 → VAD → ASR → 翻译 → 悬浮字幕 │
   └──────────────────────────────────────────────┘
```

**核心矛盾**：两个程序的音频混在同一条声卡输出流里，必须先"分流"再识别。
这正是构想 1 要解决的问题，也是整个项目的技术地基。

**明确的非目标**（本期不做，避免范围失控）：
- 不做系统级/麦克风录音转写（那是通用会议转写软件）
- 不做语音合成回放（TTS）
- 不做音频搬运（虚拟声卡、把游戏声音引到别处）
- 不做视频平台字幕下载

---

## 1. 环境实测结论（本机真实数据，非假设）

| 项目 | 实测结果 | 对项目的影响 |
|---|---|---|
| 操作系统 | Windows 11 **25H2**（Build 10.0.26200） | 满足进程回环要求的 20H1+；可用 Fluent/新 API |
| CPU | AMD Ryzen 9 **9950X**（16C/32T） | CPU 档位实际性能远超"低端"，仅作开发基准 |
| 内存 | **93.6 GB** | 可常驻 whisper large 系列模型 |
| 独显 | NVIDIA **RTX 5090**（驱动 616.56，显存 32GB） | Blackwell 架构 → CUDA 需 **12.8+**，老 wheel 不可用 |
| 核显 | AMD Radeon Graphics（iGPU） | 多 GPU 环境，需显式指定设备号 |
| 虚拟显示 | GameViewer Virtual Display Adapter | 多屏/DPI 逻辑要按 per-monitor 处理 |
| Python | **仅 3.14.2**（`pythoncore-3.14-64`） | ⚠️ **重大陷阱**，见下 |
| uv | **0.9.18** 已装 | 用它拉独立解释器 |
| git | 2.54.0，身份 `NaughtDZ`（邮箱略） | 可直接 init/push |
| 代理 | `127.0.0.1:2333` **连通**（TCP 通） | pip/uv/模型下载全部走它 |

### 1.1 ⚠️ 最高优先级陷阱：Python 3.14

本机全局只有 Python **3.14.2**。但：

- `proc-tap`（音频采集地基）：官方 README 明确写 **Windows + Python 3.13+ 可能编译失败**，有 cp310~cp314 轮子但 3.13+ 在 Windows 上被标注为风险项
- `sherpa-onnx 1.13.8`：PyPI 上 win 轮子覆盖 **cp310 / cp311 / cp312**，3.14 轮子未出现在主流列表
- `PySide6 6.11.2`：虽然 `cp310-abi3` 轮子理论上能吃 3.14，但整条 ML 依赖链不会一起配合

**结论（强制约定）**：
> 项目**不使用系统 Python 3.14**。用 `uv` 在工作目录创建 **Python 3.12** 独立虚拟环境 `.venv/`，所有依赖只装进该 venv。
> 禁止任何形式的全局 `pip install`。

---

## 2. 技术选型（逐条回应你的三个构思）

### 2.1 回应构思 1：怎么"只抓小说软件的音频"

**方案对比**

| 方案 | 能否单程序隔离 | 是否需装驱动 | 兼容性 | 结论 |
|---|---|---|---|---|
| **WASAPI 进程回环**（`AUDIOCLIENT_ACTIVATION_TYPE_PROCESS_LOOPBACK`） | ✅ 按 PID 精确隔离 | ❌ 不需要 | Win10 20H1+ / Win11 | ✅ **采用** |
| 虚拟声卡（VB-Cable 等）+ 让小说软件单独输出 | ✅ | ✅ 要装驱动、要改各软件输出设备 | 麻烦，用户门槛高 | ❌ 备选 |
| 全设备回环 + 后期音频分离（声源分离） | ❌ 不可靠 | ❌ | 质量差 | ❌ 否决 |
| 靠用户手动在音量合成器里把游戏静音 | ⚠️ 破坏游戏体验 | — | — | ❌ 不采用（用户要边听边打游戏） |

**采用实现**：Python 库 **`proc-tap` v1.1.1**

- 底层：Windows 原生 C++ 扩展 + `ActivateAudioInterfaceAsync`（现代 WASAPI 进程回环）
- 许可：**MIT**（可商用、可闭源分发，无授权风险）
- 权限：**无需管理员**
- 输出：48 kHz / 2ch / float32（⚠️ 官方文档自相矛盾，另一处写 44.1 kHz；**P1 以 `get_format()` 实测为准**）
- 同时支持 `ProcessAudioCapture(pid).start()` 回调式 与 `read(timeout=)` 同步式

> ⚠️ **实测更正（2026-02-21，详见 `docs/P0-实测记录.md`）**
> proc-tap 自带的 `--list-audio-procs` **不可用**：它在 Windows 上只是拿进程名去正则匹配
> `audio|media|music|player|chrome|...`，或"列出有窗口的进程再按关键词过滤"，
> **完全没有调用任何音频会话 API**（尽管其 docstring 声称用了 Windows Audio Session API）。
> 因此 **"选择音频来源程序"下拉框必须自行实现**：
> 采用 **`pycaw`**（Python Audio Control Library）调用
> `IAudioSessionManager2` / `IAudioSessionEnumerator`，即音量合成器背后的同一套接口。✅ 已实现
>
> 另外 proc-tap 的 README 写 `from proctap import ProcTap`，**实际类名是 `ProcessAudioCapture`**。

**⚠️ P1 阶段必须实测的 5 个未知点**（这些不验证就写不出正确代码）：
1. 目标程序在音量合成器里**被调音量/静音**，是否影响采集到的信号？（影响 UI 提示文案）
2. 小说软件**暂停播放**时，采集到的是"静音流"还是"无数据流"？（影响 VAD/超时管理）
3. 目标进程**重启**（PID 变化）后如何自动重连？（必须支持按**进程名**跟随）
4. 目标进程切换输出设备（如切到蓝牙耳机）时是否中断？
5. 采集是否受 Windows "独占模式"输出设备影响？

### 2.2 回应构思 2：实时字幕引擎（低 / 中 / 高端全覆盖）

**你的直觉是对的，但不能只押一家。** 现实是：

- Whisper 系列：**中文准确率中等**，且有"幻觉/自动补词/繁简混乱"问题，但它 GPU 兼容面最广（Vulkan 通吃 N/A/I）
- sherpa-onnx 系列：**轻、快、真流式**，中文专用模型（Paraformer / SenseVoice）效果比 Whisper 更贴合"听小说"场景
- Windows 内置 Live Captions：零成本，但**没有公开 API**，只能靠 UI Automation 抓窗口文本，非常脆弱

**三档引擎矩阵**

| 档位 | 目标设备 | 引擎 | 模型 | 端到端延迟 | 显存/内存 |
|---|---|---|---|---|---|
| **低端** | 无独显 / 老核显 / 8GB 内存 | sherpa-onnx 流式 zipformer-small **int8**（CPU） | 20~80 MB | 300~800 ms | < 1 GB |
| **中端** | 入门独显 / 较新核显 | sherpa-onnx **SenseVoice-Small int8** 或 Paraformer-zh（ONNX，DirectML/CPU） | ~230 MB | 200~600 ms | 1~2 GB |
| **高端** | 独显 6GB+ | **whisper.cpp large-v3-turbo**（Vulkan / CUDA 二选一）+ VAD 分块 | 0.8~1.6 GB | 500~1500 ms | 2~4 GB |
| **兜底** | 想零安装 | Windows Live Captions 桥接（UIA 读文本） | 0 | 系统级 | 0 |

> 「高端档用 Whisper 而非更强中文模型」是**故意**的：高端档的定位是"音频脏、有 BGM、有音效"时更鲁棒，与中端档形成互补，而不是单纯比谁准。

**GPU 兼容性策略（回应"DirectML？"）**

DirectML **不是**银弹：`sherpa-onnx` PyPI 轮子自带它自己的 ONNX Runtime，能否切 `directml` provider **必须实测**（社区已有 "Whisper medium error with DirectML" 类问题记录）。所以采用**双通道**：

| 通道 | 覆盖硬件 | 说明 |
|---|---|---|
| **Vulkan**（whisper.cpp） | NVIDIA + AMD + Intel 全系 | 一劳永逸的"万金油"GPU 加速 |
| **ONNX Runtime**（sherpa-onnx） | CPU 保底；CUDA 给 N 卡；**DirectML 待验证** | P2 出结论，若不支持则回退 CPU int8 |
| **CUDA**（可选） | 仅 NVIDIA，需匹配 12.8+ | 本机 5090 用得上，但**不当默认** |

**统一抽象**：所有引擎实现同一接口，上层不感知差异。

```python
class ASREngine(Protocol):
    def start(self, cfg: EngineConfig) -> None: ...
    def feed(self, pcm16k_mono: np.ndarray) -> None: ...
    # 事件流：partial 不断刷新，final 定稿后送翻译
    def events(self) -> Iterator[ASREvent]: ...   # Partial / Final / Error
    def close(self) -> None: ...
```

**VAD 是性能关键**：用 Silero-VAD（ONNX）做语音活动检测，静音段完全跳过 ASR → 低端机 CPU 占用能降一个量级，同时天然给出断句点。

### 2.3 回应构思 3：翻译层（传统 API + LLM + 本地）

**统一适配器接口**

```python
class Translator(Protocol):
    name: str
    supports_streaming: bool
    def translate(self, reqs: list[TranslateReq], ctx: TranslateCtx) -> list[str]: ...
    def translate_stream(self, req, ctx) -> Iterator[str]: ...  # LLM 用
```

**通道矩阵**

| 类型 | 引擎 | 鉴权 | 流式 | 备注 |
|---|---|---|---|---|
| 传统 | 百度翻译 | appid + secret（MD5 签名） | ❌ | 国内稳 |
| 传统 | 有道智云 v3 | appKey + appSecret（SHA256） | ❌ | 质量好 |
| 传统 | 微软 Azure Translator | `Ocp-Apim-Subscription-Key` + region | ❌ | 免费额度大 |
| 传统 | 谷歌翻译 API | API Key（官方）/ 免费端点（**非官方，随时失效**） | ❌ | 标注风险 |
| 传统 | DeepL | API Key | ❌ | 质量高、有免费档 |
| 传统 | 腾讯 / 彩云 / 火山 | 各自签名 | ❌ | 可选扩展 |
| LLM | OpenAI 兼容（OpenAI/DeepSeek/Moonshot/通义/智谱/OpenRouter/one-api） | `base_url` + `api_key` | ✅ | **统一入口**，一套代码通吃 |
| LLM | Ollama | `127.0.0.1:11434` | ✅ | 本地 |
| LLM | **LM Studio** | `127.0.0.1:1234`（OpenAI 兼容） | ✅ | **你本机已在跑，零配置可用** |
| LLM | llama.cpp server | `127.0.0.1:8080` | ✅ | 本地 |

**翻译质量的关键设计（不只是"调个 API"）**

1. **上下文连贯**：把前 N 句原文+译文一起进 prompt，解决代词、称谓、语气漂移
2. **术语表强制映射**：人名/地名/功法名/专有名词 → 译前替换 + 译后校正（小说场景刚需）
3. **提示词模板系统**：内置「字幕直译」「字幕意译」「网文口语风」「保留术语」等，且**全部可编辑/可新增**；模板变量支持 `{glossary}` `{context}` `{source}`
4. **不做内容审查/软化**：内置提示词明确要求"忠实翻译，不得删改、概括、弱化"，涉及成人向内容按原文如实呈现
5. **翻译缓存**：`hash(原文+引擎+模板+术语表)` 作 key 存 SQLite → 重复句不重复计费（听书有大量重复口头禅/章节标题）
6. **批量合并**：把短时间内多句打包成一次请求（LLM 尤其有效），降低 QPS 压力
7. **降级链路**：LLM 失败 → 传统 API → 仅显示原文（绝不阻塞字幕显示）
8. **成本护栏**：并发上限、QPS 限流、失败退避重试、每日字符量统计

**双语显示**：原文 / 译文 / 双语（原上译下、或同行）三种模式，字号、行高、颜色独立配置。

### 2.4 悬浮窗与"永远在最前端"

**实现**：PySide6（Qt6），窗口标志组合

```
Qt.FramelessWindowHint            无边框
Qt.WindowStaysOnTopHint           置顶
Qt.Tool                           不出现在任务栏/Alt-Tab
Qt.WindowDoesNotAcceptFocus       不抢焦点
Qt.WA_TranslucentBackground       真透明背景
+ Win32 附加样式（ctypes）：
    WS_EX_LAYERED | WS_EX_TRANSPARENT  → 点击穿透
    WS_EX_NOACTIVATE                   → 绝不激活
+ 定时器重申 SetWindowPos(HWND_TOPMOST)  → 对抗游戏/其他软件抢置顶
```

**全屏兼容性的现实边界（必须诚实告知用户）**

| 游戏显示模式 | 能否覆盖 | 方案 |
|---|---|---|
| 窗口化 / 无边框全屏 | ✅ **完全可行** | 置顶 + 分层窗口 + 定时重申 |
| "独占全屏"但**开着**全屏优化（FSO） | ✅ 通常可行 | Win10+ 会把其转为 flip 模型，置顶窗口仍能覆盖 |
| **真·独占全屏**（禁用全屏优化 / DXGI 独占） | ❌ **普通窗口无法覆盖** | 见下方三条出路 |

真独占全屏的出路（按推荐度）：
1. **推荐**：引导用户在游戏设置里改成"无边框窗口全屏"（实测性能损失通常 < 2%，且切屏更爽）
2. **第二屏 / 手机镜像**：本程序内置轻量 HTTP 服务，手机或副屏浏览器打开即看字幕（低端设备也能用）
3. **可选高级模块**：DXGI Present Hook 注入式 Overlay（如 RTSS 原理）——**风险极高**：可能触发反作弊封号。**默认不实现**，仅在用户明确知情并要求时作为独立可选组件，且不随主程序分发。

**渲染细节（决定"能不能看清"）**
- 文字**描边/阴影**（QPainterPath + QPen），保证在亮/暗游戏画面上都可读
- **滚动模式**：① 累积上滚（经典字幕） ② 单行替换 ③ 打字机逐字 ④ 卡拉OK逐字高亮（仅在有词级时间戳的引擎上启用，否则自动降级）
- 字体：内置思源黑体 / 霞鹜文楷等可商用字体；支持自定义字体文件
- 每显示器独立 DPI 缩放；多屏可选"跟随游戏所在屏"
- 透明度、背景色/背景模糊、最大行数、行间距、淡入淡出动画

---

## 3. 系统架构

### 3.1 数据流

```
┌──────────────┐
│ 目标进程(PID) │  ← 用户在 UI 里从"正在发声的程序"列表中选中
└──────┬───────┘
       │ WASAPI 进程回环 (proc-tap)  48k/2ch/f32
       ▼
┌──────────────────┐
│ CaptureWorker    │  独立线程 · 回调写环形缓冲
└──────┬───────────┘
       ▼
┌──────────────────┐
│ AudioPipeline    │  降混单声道 → 重采样 16k (soxr) → 归一化
└──────┬───────────┘
       ▼
┌──────────────────┐
│ VAD (Silero)     │  静音跳过 · 输出语音段
└──────┬───────────┘
       ▼
┌──────────────────┐
│ ASREngine        │  流式: Partial/Final 事件；分块: 整段送模型
└──────┬───────────┘
       │ ASREvent(partial/final)
       ├──────────────────────────────┐
       ▼                              ▼
┌──────────────┐            ┌──────────────────┐
│ SubtitleStore│            │ TranslatorHub    │  final 才翻译
│ (状态+历史)   │◀───────────│ 缓存/限流/术语表  │
└──────┬───────┘            └──────────────────┘
       ▼
┌──────────────────┐
│ OverlayWindow    │  Qt 主线程 · 30fps 从状态取最新字幕重绘
└──────────────────┘
       +
┌──────────────────┐
│ SettingsWindow   │  PySide6 配置界面 · 托盘 · 热键
└──────────────────┘
```

### 3.2 线程模型与背压策略

| 线程 | 职责 | 说明 |
|---|---|---|
| Qt 主线程 | UI、悬浮窗渲染、热键、托盘 | 绝不阻塞，只读状态快照 |
| Capture 线程 | proc-tap 回调 | 只做"拷贝进环形缓冲"，不做任何计算 |
| Audio/VAD 线程 | 降混、重采样、VAD | CPU 密集但极短 |
| ASR 线程 | 推理 | **单实例串行化**（onnxruntime/whisper 不保证线程安全） |
| 翻译线程池 | 网络/LLM 调用 | 并发 N（默认 4），带限流与重试 |

**背压原则**：字幕是"实时消费品"，**宁可丢弃过期中间结果，不可无限堆积**。
- Partial 事件：新旧覆盖式写入（丢掉没人看得到的中间态）
- Final 事件：进有界队列（容量 64），满则告警并丢弃最旧
- 翻译结果：允许乱序回来，按 segment id 归位；超过 8 秒的译文回来直接丢弃（已滚出屏幕）

### 3.3 延迟预算（目标）

| 环节 | 低端 | 中端 | 高端 |
|---|---|---|---|
| 采集 + 重采样 | 10~30 ms | 同 | 同 |
| VAD 断句判定 | 150~300 ms | 150~300 ms | 150~300 ms |
| ASR | 300~800 ms | 200~600 ms | 500~1500 ms |
| 翻译（传统 API） | 100~400 ms | — | — |
| 翻译（LLM 首 token） | 200~1500 ms | — | — |
| **原文首屏** | **< 1.0 s** | < 0.9 s | < 1.8 s |
| **译文追加** | 原文定稿后 +0.3~1.5 s | 同 | 同 |

> 设计原则：**原文先出，译文后补**，绝不为了等译文而让字幕空着。

---

## 4. 目录结构

```
ListenAndShowAndTranslate/
├── 计划书.md
├── README.md
├── LICENSE                      (MIT)
├── .gitignore                   ★ 红线：.venv/ 与 data/ 必须忽略
├── .python-version              (3.12)
├── pyproject.toml               (uv 管理)
├── requirements.txt             (兼容 pip 的冻结清单)
├── start.bat                    (一键启动：uv run python main.py)
├── main.py
├── app/
│   ├── __init__.py
│   ├── config.py                配置模型 (pydantic) + 读写 + 迁移
│   ├── paths.py                 数据/模型/缓存目录 (platformdirs)
│   ├── audio/
│   │   ├── process_list.py      枚举正在发声的进程（音量合成器等价）
│   │   ├── capture.py           proc-tap 封装 + 进程跟随重连
│   │   ├── pipeline.py          降混/重采样/环形缓冲
│   │   └── vad.py               Silero-VAD
│   ├── asr/
│   │   ├── base.py              ASREngine 抽象 + 事件定义
│   │   ├── sherpa_stream.py     流式 zipformer
│   │   ├── sherpa_offline.py    SenseVoice / Paraformer
│   │   ├── whispercpp.py        whisper.cpp 子进程封装
│   │   ├── faster_whisper.py    NVIDIA 可选
│   │   └── livecaptions.py      Windows Live Captions 桥接（兜底）
│   ├── translate/
│   │   ├── base.py              Translator 抽象 + 请求/上下文模型
│   │   ├── traditional/         baidu / youdao / azure / google / deepl ...
│   │   ├── llm/                 openai_compat / ollama / lmstudio / llamacpp
│   │   ├── prompts/             内置提示词模板 (yaml)
│   │   ├── glossary.py          术语表
│   │   └── cache.py             SQLite 翻译缓存
│   ├── subtitle/
│   │   ├── model.py             字幕段/行状态机
│   │   ├── store.py             历史、导出 srt/txt
│   │   └── renderer.py          描边绘制、滚动模式、动画
│   ├── ui/
│   │   ├── overlay.py           悬浮字幕窗
│   │   ├── settings/            配置界面（分页：音频/识别/翻译/外观/关于）
│   │   ├── tray.py              托盘菜单 + 全局热键
│   │   └── wizard.py            首次运行向导（硬件探测→推荐档位→下模型）
│   ├── models/
│   │   ├── registry.py          模型清单（名称/大小/来源/校验值）
│   │   ├── downloader.py        断点续传 + 代理 + HF镜像/ModelScope
│   │   └── hardware.py          硬件探测与档位评分
│   └── utils/
│       ├── win32.py             ctypes：置顶/点击穿透/DPI/枚举窗口
│       ├── net.py               代理注入、重试退避
│       ├── log.py               滚动日志
│       └── events.py            进程内事件总线
├── assets/
│   ├── fonts/                   (可商用字体，注意许可)
│   ├── icons/
│   └── prompts/                 用户可覆盖的提示词模板
├── tests/
│   ├── test_audio_pipeline.py
│   ├── test_process_list.py
│   ├── test_translate_adapters.py   (全部 mock，不打真网络)
│   └── test_subtitle_store.py
├── scripts/
│   ├── bench_asr.py             各引擎 RTF/延迟基准
│   └── gen_test_audio.py        生成测试音频
└── docs/
    ├── 架构.md
    ├── 常见问题.md
    └── 全屏兼容性.md
```

**运行时目录（不进 Git）**
```
data/
├── config.json          用户配置（含 API Key → 绝不提交）
├── listen.db            SQLite：历史字幕 / 翻译缓存 / 统计
├── models/              下载的模型
├── logs/
└── output/              导出的 srt / txt
```

---

## 5. 技术栈与依赖

**解释器**：Python **3.12**（由 uv 管理，独立于系统 3.14）

| 依赖 | 用途 | 备注 |
|---|---|---|
| `PySide6` | UI + 悬浮窗 | abi3 轮子，兼容面广 |
| `proc-tap` | 进程级音频采集 | ★ 项目地基，MIT |
| **`pycaw`** | **枚举发声进程（音频会话 API）** | **音量合成器同款接口，不可用 proc-tap 的版本替代** |
| **`comtypes`** | pycaw 的 COM 绑定依赖 | |
| **`psutil`** | PID → 进程名/路径 | pycaw 会话对象即 psutil.Process |
| `sherpa-onnx` | 流式/离线 ASR + VAD | Apache-2.0 |
| `onnxruntime-directml` | DirectML 推理（中端） | **P2 实测后决定是否启用** |
| `numpy` | 音频数组 | |
| `soxr` 或 `libsamplerate` | 高质量重采样 | 低延迟优先 |
| `soundfile` | 测试音频读写 / 导出 | |
| `httpx` + `tenacity` | HTTP + 重试退避 | 统一走代理 |
| `pydantic` | 配置模型校验 | |
| `platformdirs` | 跨平台目录 | |
| `rich` (可选) | 控制台日志 | |
| `pyinstaller` 或 `nuitka` | 打包（P6） | 打包体积需实测 |

**外部二进制（非 pip，由下载器管理）**
- `whisper.cpp` 预编译二进制：`cublas`(CUDA 12.8+) / `vulkan` / `cpu` 三个变体
- 可选 `ffmpeg`：测试音频生成与格式转换

---

## 6. 里程碑计划

> 工时按"1 名开发者 + AI 协助"估算；每个阶段都有**可验收的产出**，不是"写完代码就算完"。

### P0 · 工程基建（0.5 天）✅ 已完成 2026-02-21
- `uv venv` 创建 **Python 3.12.12** 环境；配置代理（pip / uv / httpx 三处）
- 建目录骨架、`.gitignore`（**红线：`.venv/`、`data/`、`*.db`、模型文件、含 Key 的配置**）
- `pyproject.toml` + `requirements.txt` + `requirements-dev.txt` + `start.bat`
- `git init` + 首次 commit + 推送 GitHub 仓库
- **验收（全部通过）**：
  - `main.py --selftest` 15 项全 ✓（含代理连通、GPU 探测）
  - `main.py --list-audio` 真实列出 `GenshinImpact.exe（原神）`、`msedge.exe`
  - `pytest` 19 项通过
  - `git ls-files` 不含 `.venv/`、`data/`；索引扫描无常量密钥

### P1 · 音频链路验证（1~2 天）★ 最高风险，优先做
- 枚举正在发声的进程（含进程名/图标/PID）
- 选择目标 PID → 采集 → 实时电平表 → 落盘 wav 回放
- 进程跟随（PID 变化自动重连）、异常恢复
- **验收**：同时播放小说音频 + 游戏音频，录出的 wav **只有小说声**（波形/频谱+人耳双确认）；第 2.1 节列的 5 个未知点全部有实测结论并记录成文档

### P2 · ASR 引擎抽象 + 多语言路由（5~7 天，因多语言需求而上调）
- `ASREngine` 接口 + 事件流（partial/final）+ VAD 接入；**接口必须同时容纳流式与分块引擎**
- **语言路由**：实现 `app/asr/router.py`，按 `asr.language` / LID 结果选引擎
  - 流式引擎：sherpa-onnx 流式 zipformer（zh / zh-en / en）
  - 分块引擎：sherpa-onnx SenseVoice（ja / ko / yue，**一个模型覆盖 5 语言**）
  - Whisper 兜底：whisper.cpp（小语种，Vulkan/CUDA）
- **语种识别（LID）**：接 `SpokenLanguageIdentification`（Whisper-tiny），
  支持 `language=auto`；带置信度阈值与结果缓存，避免句间来回跳
- **降级链**：目标语言模型缺失 → SenseVoice → Whisper → 明确报错（不静默失败）
- **模型下载器**（P2 内先做最小可用版）：代理 + HF 镜像 + ModelScope + 断点续传 + SHA 校验
- 实测 DirectML provider 是否可用（结论写进 `docs/P2-ASR实测.md`）
- `scripts/bench_asr.py` 输出**按语言分组**的 RTF、P50/P95 延迟、内存/显存占用
- **验收**：
  - 中文小说音频（**含背景音乐与音效**）连续跑 30 分钟不崩
  - **中 / 英 / 日 三种语言各跑一遍**，延迟与占用有数据报告（日语延迟预计高于中文，属已知取舍）
  - `language=auto` 能在中英日之间正确切换
  - 缺模型时走降级链并在 UI 明确提示，而不是静默出错

### P3 · 悬浮字幕窗（2~3 天）
- 无边框 / 透明 / 置顶 / 点击穿透 / 不抢焦点 / 不出现在任务栏
- 文字描边渲染 + 4 种滚动模式 + 字体字号行距配置
- 多显示器 + DPI 缩放 + 热键（开关穿透、切显示模式、暂停）
- **验收**：窗口化游戏与无边框全屏游戏下字幕清晰可读，且**键鼠操作完全不受影响**；真独占全屏下能给出准确提示与替代方案

### P4 · 翻译层（3~4 天）
- `Translator` 接口 + 4~6 个传统 API 适配器 + 2 条 LLM 通道（OpenAI 兼容 / Ollama+LM Studio）
- 提示词模板系统（内置多套、可自定义、变量注入）
- 术语表、上下文连贯、SQLite 缓存、批量合并、限流重试、降级链路
- 双语显示（原/译/双语三模式）
- **验收**：断网/错误 Key 不崩溃且有明确提示；缓存命中不重复计费；双语对齐显示正确；成人向内容不被软化或删改

### P5 · 模型管理与档位预设（1~2 天）
- 硬件探测 → 推荐档位 → 一键下载模型
- 下载器：代理、HF 镜像 / ModelScope、断点续传、SHA 校验、进度显示
- 低/中/高三档预设与"自动测速选档"
- **验收**：在干净机器上从零到能出字幕，全程无需手动下载模型

### P6 · 打包与体验打磨（2~3 天）
- 打包（PyInstaller/Nuitka）+ `start.bat` + 首次运行向导
- 系统托盘、全局热键、日志滚动、崩溃转储
- 字幕历史查看与 `srt`/`txt` 导出
- 设置导入导出（**导出时剔除 API Key**）
- **验收**：非开发者也点得动；打包版体积与启动时间可接受

### P7 · 加分项（按需，不阻塞发布）
- 手机/平板/副屏同屏（本机 HTTP + 浏览器看字幕）→ **同时是真独占全屏的替代方案**
- 卡拉OK逐字高亮（依赖词级时间戳）
- 双引擎融合（低端引擎出快稿，高端引擎出定稿，替换式刷新）
- 音频设备热切换、录音回放、字幕样式预设分享

**关键路径**：P0 → **P1（最大不确定性）** → P2 → P3 → P4 → P5 → P6
**可并行**：P3（UI）与 P2/P4（引擎/翻译）可由不同人并行

---

## 7. 性能档位预设（交付给用户的选择）

| 预设 | 硬件画像 | ASR | 翻译 | 视觉 |
|---|---|---|---|---|
| **极致轻量** | 无独显 / 4 核 / 8GB | sherpa-onnx zipformer-small int8 (CPU) | 传统 API 或小 LLM | 单行替换，无动画 |
| **平衡（默认）** | 入门独显 / 6 核 / 16GB | SenseVoice-Small int8 (DirectML/CPU) | 传统 API + LLM 兜底 | 累积上滚 + 淡入 |
| **画质优先** | 独显 8GB+ / 32GB | whisper.cpp large-v3-turbo (Vulkan/CUDA) + VAD | 本地大模型 / 云端 LLM | 双语 + 逐字高亮 |
| **零安装** | 任意 Win11 | Windows Live Captions 桥接 | 任意 | 基础 |

---

## 8. 风险登记册

| # | 风险 | 概率 | 影响 | 对策 |
|---|---|---|---|---|
| R1 | 进程回环采集在某些软件上抓不到声音 | 中 | **致命** | P1 优先验证；准备"设备回环 + 让用户静音游戏"的降级路径 |
| R2 | 真独占全屏无法覆盖 | **高** | 中 | 已明确为现实限制；提供无边框引导 + 手机/副屏镜像；注入式方案默认不做 |
| R3 | DirectML 在 sherpa-onnx 上不可用 | 中 | 中 | CPU int8 保底 + Vulkan(whisper.cpp) 通道；P2 出结论 |
| R4 | Python 3.14 导致依赖装不上 | **高**（已确认） | 高 | **强制 uv 建 3.12 venv**，P0 就固化 |
| R5 | Whisper 中文幻觉（音乐/音效处乱编） | 高 | 中 | 优先 sherpa 中文模型；VAD 严格门控；`no_speech_thold` 等参数调优 |
| R6 | 低端机实时性不达标 | 中 | 中 | VAD + int8 + 小模型；降级到"延迟字幕"模式；Live Captions 兜底 |
| R7 | 翻译 API 计费失控 | 中 | 中 | 缓存 + 限流 + 每日字符统计 + 成本上限告警 |
| R8 | API Key 泄露进 Git | 中 | **高** | `.gitignore` 红线 + P0 验收 `git ls-files` + 导出配置剔除 Key |
| R9 | 模型/依赖下载失败（网络） | 中 | 中 | 全局代理 2333 + HF 镜像 + ModelScope 双源 + 断点续传 |
| R10 | 反作弊误判（若做注入式 Overlay） | 低 | **极高** | 注入模块默认不实现、不随主程序分发 |
| R11 | 字体/模型许可证问题 | 低 | 中 | 只用可商用字体与 MIT/Apache-2.0 模型，附许可清单 |
| R12 | 音频采集与游戏争抢 CPU 导致掉帧 | 低 | 中 | 采集线程低优先级、VAD 门控、可配置推理节流 |

---

## 9. 工程红线（强制约定）

1. **虚拟环境隔离**：一切 Python 依赖只装进 `.venv/`（uv 创建，Python **3.12**）。**禁止全局 pip install**。
2. **绝不进 Git**：`.venv/`、`data/`（含 `listen.db`、模型、日志、导出）、任何含 API Key / Token 的文件、代理凭据。
3. **网络**：pip / uv / 模型下载 / 翻译 API 全部支持走 `http://127.0.0.1:2333`。
4. **凭据**：API Key 只存本机 `data/config.json`，导出配置时自动剔除；日志中对 Key 打码。
5. **内容忠实**：翻译模块不做内容审查与软化，忠实还原原文（用户自带内容）。
6. **不猜**：涉及 API 行为、provider 支持、模型效果的结论，一律以**实测数据**写进 `docs/`，而不是凭印象写代码。

---

## 10. 已确认决策（2026-02-21 拍板）

| # | 决策项 | 结论 |
|---|---|---|
| D1 | UI 技术栈 | **PySide6 原生桌面** —— 透明/点击穿透/置顶只有原生窗口能可靠实现 |
| D2 | GitHub 仓库 | **Public 公开**（2026-09-12 从 Private 改为公开）：<https://github.com/NaughtDZ/ListenAndShowAndTranslate> |
| D3 | 首条 ASR 路线 | **sherpa-onnx 流式 zipformer**（先跑通低延迟闭环，再上重模型） |
| D4 | 加分项 | **纳入排期**：字幕历史与 srt/txt 导出、首次运行向导 |
| D5 | 手机/副屏镜像 | **不做** → 因此真·独占全屏**没有**覆盖方案，UI 中须明确引导用户改「无边框窗口全屏」 |
| D6 | 卡拉OK 逐字高亮 | **不做** |
| D7 | 注入式 Overlay | **不做**（反作弊封号风险，不随主程序分发） |
| D8 | 中文 / 中英 | 默认**中英都要**（`language=auto`）——按计划书建议执行，未单独确认 |

---

## 11. 执行进度

| 阶段 | 状态 | 产出 |
|---|---|---|
| P0 工程基建 | ✅ **已完成**（2026-02-21） | Python 3.12 venv、依赖清单、配置模型、日志脱敏、发声进程枚举、自检 CLI、19 项测试、Git 仓库 |
| P1 音频链路验证 | 🔬 **核心已通过，工程已落地** | ✅ 隔离实验：目标音 99.90% / 干扰 0.00%；✅ 端到端保真：注入 660Hz → 16k 单声道 WAV，主频 660.0Hz、时长精确 3.00s。已交付 `pipeline.py` / `capture.py` / `--capture` CLI / 39 项测试。剩余 3 项需人工配合（音量与静音影响、输出设备切换、非默认播放设备） |
| P2 引擎抽象 + 流式 ASR | ✅ **已完成** | 流式 zipformer（zh/zh-en/en）+ SenseVoice 离线（zh/en/ja/ko/yue）+ Whisper turbo + Silero VAD + 语言 ID 路由；中/英/日准确率与 RTF 见 `docs/P2-ASR实测.md` |
| P3 悬浮字幕窗 | ✅ **已完成** | 透明/置顶/点击穿透/不抢焦点/可拖拽缩放/字号自适应，原文·译文·双语三种显示；延迟旋钮见 `docs/延迟调节.md` |
| P4 翻译层 | ✅ **已完成** | 统一适配器 + 5 家官方 API + 谷歌/必应网页通道 + OpenAI 兼容（LM Studio/Ollama）+ 术语表 + 缓存 + 三级降级；实测见 `docs/P4-翻译实测.md` |
| P5 模型管理与向导 | 🔬 **大部分完成** | 模型注册表/下载器/硬件探测、5 页首次运行向导、设置窗（网络/翻译/识别/外观）已交付；**剩余**：识别模型手动指定、SRT 导出接线 |
| P6 打包与打磨 | ⏳ 未开始 | exe 打包、全局热键、崩溃转储 |

### 进度补充（2026-09-12，UI 生命周期修复）

用户实测反馈：**控制窗收进托盘后，只关掉设置窗，整个程序就退出了。**

- 根因：Qt 默认 `quitOnLastWindowClosed=True`，而悬浮窗是 `Qt.Tool`（不计数）、
  控制窗 `hide()` 后也不算可见窗口 → "关设置窗"被 Qt 当成"关掉最后一个窗口"。
- 修复：新增 `app/ui/lifecycle.py` 统一退出策略（关掉自动退出、退出只走显式入口、
  设置窗复用同一实例、`saved` 只接一次）；控制窗/选择窗口/向导三处入口全部接入。
- 顺带：选择窗口「开始字幕」不再只是 `hide()`（那会留下一个看不见也点不到的
  僵尸窗口），改为 1.5 s 自检子进程后自己关闭；子进程秒退时留在界面上报错。
- 新增 `docs/窗口与退出行为.md` 与 `tests/test_ui_lifecycle.py`（11 项，
  其中"核心回归"用例在还原 bug 后会失败，已实测确认）。

### 进度补充（2026-09-12，字幕窗尺寸与动态字号）

用户反馈：**上下拖动无效、拖高拖矮都弹回原样；设置 3 行时顶上会空一行**；
建议按"基准字号 + 动态缩小"来处理放不下的内容。

- 根因：自动高度按"最大可能行数"预留（3 条 × 2 行 × 双语 = 12 行 ≈ 650px），
  实际内容常常只有 4~7 行 → 空出来的部分堆在顶上（空行）；而 `_relayout()` 里的
  `max(自动高度, 用户高度)` 又把用户拖出来的高度顶回去，且手动高度只在
  `window_height == 0` 时记录一次，之后再也不更新。
- 修复：自动高度改为**按当前内容实际行数**算（不留空行）；用户拖过的高度
  **完全听用户的**；宽度基准改用"上次真正生效的宽度"（避免小屏被夹宽时误改字号）。
- 新增 `auto_shrink_font`（默认开）：窗口装不下时把字号动态缩到刚好放得下
  （下限 = 基准的 60%），内容一少自动回到基准值；设置窗「外观」页新增
  「字幕窗高度（0 = 自动）」与「放不下时自动缩小字号」两个入口。
- 新增 `docs/字幕窗尺寸与字号.md` 与 `tests/test_overlay_layout.py`（9 项）；
  `scripts/preview_subtitle.py` 出图复核：每个模式的窗口高度都紧贴内容，无裁剪。

### 进度补充（2026-09-12，设置里的静音阈值可视化）

用户反馈：「这里静音阈值这里也画个电平表和阈值线条呗，前面启动给个 ui，这里又不给了」。

- 设置 → 识别 现在内嵌与独立电平表窗口**同一个控件**：RMS/PEAK/保持 +
  **橙色阈值线**（跟着数值实时移动）+ **蓝色实测参考线**（−66 dBFS）+
  状态（有声/静音/削波/未采集）。
- 数据来源：字幕进程直接读采集线程每块算好的 `last_rms` / `last_peak`
  （为此给 `PipelineStats` 加 `last_peak`；给 `LevelTracker` 加标量版
  `update_values`，与样本版共用同一段平滑代码，有对拍测试）；
  启动窗口没有采集，如实显示"未采集"并提示用「电平表」按钮。
- 只在设置窗可见时轮询（100ms），异常不影响设置界面。
- 顺手把电平表量程从 −70~0 改成 **−90~0**：默认阈值 −80 原来看不到，
  阈值线被夹在左边缘。
- 新增 `docs/静音阈值与电平表.md` 与 `tests/test_settings_meter.py`（13 项），
  全套 303 项通过；真机渲染 `settings_asr_tab_live.png` 肉眼复核排版。

### 进度补充（2026-09-12，前端 exe + 仓库转为公开）

用户反馈：「即便缩小托盘，那个启动 bat 还是会独立一个窗口存在，很丑陋」。

- 新增 `frontend.py`（前端启动器）+ `scripts/build_exe.py` + `构建exe.bat`，
  用 PyInstaller 打成根目录的 **`听显译.exe`**（onefile + noconsole，约 8 MB）：
  启动器只用标准库，检查 `.venv` 后以 `DETACHED_PROCESS` 拉起
  `pythonw.exe main.py` 并立刻退出 —— **全程没有控制台窗口**，参数可透传
  （`听显译.exe --settings`）。没装环境时弹系统对话框一键转 `首次安装.bat`。
- 说明：它是**前端壳**而不是独立发行版，业务仍在 `.venv` + `main.py`；
  未签名（SmartScreen 可能提示）。打包依赖进 `requirements-dev.txt`（只装 .venv）。
- **仓库由 Private 改为 Public**（决策 D2 已更新）。公开前做了全历史扫描：
  无密钥类串、无 `data/`/`.venv/`/模型/日志入库；顺手把计划书里出现的作者邮箱
  改为「邮箱略」。`README.md` 整体重写：启动方式三入口对比、功能状态表改成
  与实际一致、安装改为 clone 流程、补上文档索引与目录结构。
- 新增 `tests/test_frontend.py`（10 项，纯逻辑），全套 **313 项**通过。

### 进度补充（2026-09-12，向导可重新运行）

用户反馈：「用了几次之后想重新跑首次运行向导来改模型下载，但没有入口」——
以前只有 `first_run_done` 为假时才会自动跑向导，之后想换语言包只能手改配置文件。

- 补三个入口：启动窗口「向导…」按钮、设置 → 识别 →「重新运行首次运行向导…」、
  `听显译.exe --wizard`（命令行本来就有，但界面上一直没有）。
- 关键不是入口而是**重跑不许把设置清回默认值**：向导原本按首次运行设计，
  直接重跑会把代理清空、把翻译通道换成 LLM、把延迟档位改回硬件推荐。
  现在 `FirstRunWizard` 有 `rerun` 概念并**自动判断**（`first_run_done` 为真即重跑）：
  语言包默认勾"已经装在本地"的那些（提示写明"其中 N 个已经装好，会自动跳过"）、
  档位取当前 `asr.latency_preset`、代理与翻译通道按现状预填；
  向导下拉框表示不了的官方 API 通道（百度/有道/Azure/谷歌/DeepL）
  会插一条「保持当前：…」原样保留。
- 从设置窗进入时，完成后发 `saved` → 控制窗立刻重建识别/翻译引擎；
  取消则什么都不改。延迟档位与五条旋钮会回读同步。
- 新增 `docs/向导与重新配置.md` 与 `tests/test_wizard_entry.py`（15 项），
  全套 **326 项**通过；真机用作者现有配置复核了预填结果（出图在
  `data/selftest_audio/wizard_rerun_page*.png`，过程中不保存、不下载）。

### 进度补充（2026-09-12，识别模型可手动指定）

用户要求：「既然模型是程序硬编码好的会下载哪些模型，那么哪些模型支持哪些语言，
程序应该是知道的，这样不是不会产生用户乱下载模型然后乱用的情况吗？」

- 在 `app/models/registry.py` 明确"谁支持谁"：新增 `ASR_KINDS`、`LANGUAGE_LABELS`、
  `supports_language()`、`models_for_language()`、`route_kwargs_for()`、
  `pack_for_model()`；语种识别模型（lid）与 VAD 一律不算识别模型。
- 设置 → 识别 新增「识别模型（按语言）」：每个语言一行下拉框（中文/中英混说/
  英语/日语/韩语/粤语/其它语言兜底），候选**只有真的支持这门语言的识别模型**；
  引擎类型由注册表决定（用户不必判断流式还是分块）；未下载的标「（未下载）」
  并提示点哪个语言包补下；「全部恢复默认」一键回自动。
- 运行时兜底：`LanguageRouter._active_route()` 检查配置里的模型（存在性 / 是否识别
  模型 / 是否支持该语言），不能用就退回默认并说明原因，同一语言只提示一次。
  配合 `LanguageRoute.engine` 的 Literal 校验，手改配置文件也塞不进乱模型。
- `ASRConfig.default_route_for()` 暴露内置默认路由（界面「自动」= 恢复它）。
- 新增 `docs/识别模型选择.md` 与 `tests/test_model_choice.py`（19 项），
  全套 **347 项**通过；真机复核：ja 候选只有 SenseVoice 与 Whisper turbo、
  指定 zh→中英双语模型生效、故意填 LID 模型会被拦下并改回默认。

### 进度补充（2026-09-12，设置窗排版修复）

用户反馈：「设置页加了模型选择之后窗口会很长很长，标题栏被挤到屏幕顶部外面，
用户拖动不了了」。

- 根因是**最小高度**：`minimumSizeHint` 实测 **692×1275**（「识别」一页的最小高度
  1199px 被 QTabWidget 继承成整窗最小高度），Qt 照这个摆窗口 → 标题栏跑到屏幕外。
- 两条一起修：① 每个分页都套 `QScrollArea`（`lifecycle.scrollable`），最小高度降到
  ~70px；② 显示前 `lifecycle.fit_window_to_screen()` 把尺寸夹到屏幕的 92%、
  位置也夹回可用区域（设置窗在 showEvent 夹一次，向导在 run_wizard 里夹）。
- 顺带**拆页**：设置窗 4 页 → 5 页，新增「模型」页（识别模型按语言选择 +
  语言包下载入口），「识别」页只剩语言/静音阈值/延迟参数。
  右下角加 `QSizeGrip`，窗口可缩放一目了然。
- 新增 `tests/test_settings_layout.py`（6 项）：最小高度必须放进 768p 屏幕、
  每页可滚动、夹屏函数行为、只夹一次不跟用户较劲；全套 **357 项**通过。
- 文档：`docs/窗口与退出行为.md` 新增「窗口绝不许比屏幕大」一节；
  README 与 `docs/识别模型选择.md`、`docs/向导与重新配置.md` 的入口路径
  同步改成「设置 → 模型」。

### P1 阶段新增的硬约束（由实测得出，必须遵守）

1. **绝不能用"我拿到的 PID"当音频会话 PID** —— uv venv 的 `python.exe` 是转发器，
   真实播放进程 PID 不同。一切以音频会话枚举为准。
2. **多进程应用（浏览器/Electron 阅读器）必须按音频会话选**，不能按进程名选，
   否则会抓到不发声的子进程 → 3 秒纯静音。
3. **目标未播声时返回的是静音流而非无数据**（速率恒定 384 KB/s），
   因此判断"是否在放音"只能靠 VAD/能量阈值，**不能靠 `read()` 有没有数据**。
4. **WAV 落盘不要照抄官方示例**：真实为 48 kHz，官方示例写 `setframerate(44100)` 是错的。
5. **重采样会带来 40 ms 固定延迟且输出是突发式的**（HQ 档第 4 块才出数据，之后每 3 块吐一批）。
   这是延迟预算的固定项，不是 bug；下游必须有缓冲，不能假设回调节奏均匀。
6. **采集启动约需 0.6 s**（会话枚举 + WASAPI 激活）。
   因此"录 N 秒"必须**按已收样本数计时**，按墙钟计会少给约 0.6 秒音频。

---

## 12. 多语言方案（2026-02-21 新增需求）

> 需求：中文、英文、日语常用，偶尔还需要小语种。

### 12.1 核心判断：**没有"一个模型通吃且又快又准"的方案**

必须区分两类"多语言"：

| | 单模型多语言 | 每语言专用模型 |
|---|---|---|
| 代表 | **Whisper**（99 语言）、**SenseVoice**（5 语言） | 流式 zipformer（中/英/韩/法… 各自独立） |
| 优点 | 一个模型全覆盖，自动适配 | 延迟最低、该语言准确率最好、体积小 |
| 缺点 | 非流式（延迟高）、Whisper 易幻觉；SenseVoice 只 5 语言 | 换语言要换模型，占用要累加 |

**结论：两条都要用，按语言路由。** 常用语言走专用流式模型，冷门语言回退到 Whisper。

### 12.2 官方模型清单（已通过 HF API / 官方文档核实）

| 语言 | 引擎 | 模型 | int8 体积 | 延迟 | 备注 |
|---|---|---|---|---|---|
| 中文 | 流式 zipformer | `sherpa-onnx-streaming-zipformer-zh-int8-2025-06-30` | ~167 MB | **300~500 ms** | 最快最好 |
| 中英混说 | 流式 zipformer | `...-streaming-zipformer-bilingual-zh-en-2023-02-20` | ~207 MB | 同 | 小说夹英文词 |
| 英文 | 流式 zipformer | `...-streaming-zipformer-en-2023-06-26` | ~71 MB | 同 | |
| **日语** | **SenseVoice** | `sherpa-onnx-sense-voice-zh-en-ja-ko-yue-int8-2025-09-09` | 237 MB | 分块 800~1500 ms | ⚠️ 见 12.3 |
| 韩语 / 粤语 | SenseVoice | 同上（同一模型） | 同上 | 同上 | 与日语共用一份模型 |
| **小语种** | **Whisper** | `sherpa-onnx-whisper-turbo`(int8 ≈1.0 GB) / `large-v3`(int8 ≈1.7 GB) | 1.0~1.7 GB | 分块 1~3 s | 99 语言兜底 |
| **语种识别** | Whisper-tiny LID | `sherpa-onnx-whisper-tiny`（int8 编码器 12.9 MB + 解码器 89.9 MB） | ~103 MB | 很快 | `SpokenLanguageIdentification` |

### 12.3 ⚠️ 必须直说的取舍：**日语没有流式模型**

sherpa-onnx 的**在线（流式）模型列表里没有日语**——中文、英文、韩语、法语、孟加拉语都有，
日语只有非流式的 SenseVoice / Whisper 可用。

后果：**日语字幕延迟会明显高于中文**（中文约 0.3~0.5 s，日语约 0.8~1.5 s）。

可选改善路径（按性价比排序）：
1. **先接受**：日语用 SenseVoice 分块 + 激进 VAD 断句（这是 P2 的默认做法）
2. 日语单独缩短 VAD 静音判定（如 250 ms），牺牲断句正确率换延迟
3. 后续把 ReazonSpeech 的日语流式 zipformer 转换/导出为 sherpa-onnx 格式
   （官方有转换脚本，但属于额外工程量，不进 P2 范围）
4. 若用户接受"非实时"：日语用 faster-whisper large-v3-turbo 跑 GPU（5090 上很快），
   准确率最高但延迟 1~2 s

### 12.4 语言路由设计

```
音频 → VAD 断句 → [语种识别 LID] → 按语言选引擎 → 识别
                        │
                        ├─ zh / zh-en 混说 → 流式 zipformer（最低延迟）
                        ├─ en            → 流式 zipformer en
                        ├─ ja / ko / yue → SenseVoice（一个模型覆盖）
                        └─ 其它/不确定    → Whisper turbo（99 语言兜底）
```

- **语种识别策略**：
  - 用户**指定语言** → 跳过 LID，直接用对应引擎（最快，推荐默认）
  - 设为 `auto` → 用 Whisper-tiny LID，**每 N 句或检测到置信度低时重判**，
    支持"小说中途换语言"（多语言小说集常见）
  - LID 结果缓存：同一段音频不反复判
- **降级链**：目标语言模型缺失/加载失败 → SenseVoice → Whisper → 报错提示
- **热切换**：引擎可运行时切换，切换时不中断采集（只换 ASR 后端）

### 12.5 对翻译层的影响（新增）

ASR 现在会产出**源语言**，翻译层必须带上：

- 配置新增 `translate.source_language`（默认 `auto`，即用 ASR 判定的语言）
- 传统 API 必须传源语言：百度/有道/微软/DeepL 都支持 `ja→zh`、`ko→zh` 等
  （**注意**：部分免费/未认证额度只支持 `auto` 或英中转，需要在适配器里处理降级）
- LLM 通道天然支持任意源语言，只需在提示词里注入"源语言→目标语言"
- **日→中的坑**：日语主语/敬语省略严重，逐句翻译极易丢失说话人关系；
  提示词必须带上文（已有 `context_lines` 机制），并在术语表里支持人名映射

### 12.6 配置结构调整（已实现）

```jsonc
"asr": {
  "language": "auto",              // auto | zh | zh-en | en | ja | ko | yue | ...
  "language_detection": true,      // auto 时是否启用 LID
  "lid_model_dir": "",             // 留空用内置默认
  "routing": {                     // 语言 → 引擎/模型，用户可覆盖
    "zh":   {"engine": "sherpa_stream",  "model": "streaming-zipformer-zh-int8"},
    "en":   {"engine": "sherpa_stream",  "model": "streaming-zipformer-en"},
    "ja":   {"engine": "sherpa_offline", "model": "sense-voice-zh-en-ja-ko-yue-int8"},
    "ko":   {"engine": "sherpa_offline", "model": "sense-voice-zh-en-ja-ko-yue-int8"},
    "yue":  {"engine": "sherpa_offline", "model": "sense-voice-zh-en-ja-ko-yue-int8"},
    "*":    {"engine": "whispercpp",     "model": "whisper-turbo-int8"}
  },
  "fallback_engine": "sherpa_offline"
}
```

### 12.7 下载体积预算（首次运行向导要展示）

| 档位 | 包含 | 体积 |
|---|---|---|
| 仅中文 | 流式 zh + VAD | ~200 MB |
| 中英 | 流式 zh-en 双语 + VAD | ~240 MB |
| **中日英韩粤**（推荐默认） | 上述 + SenseVoice int8 + VAD | **~480 MB** |
| 全语言 | 再加 Whisper-turbo int8（+1.0 GB）/ large-v3 int8（+1.7 GB） | 1.5~2.2 GB |
| 语种自动识别（可选） | 再加 Whisper-tiny LID | +103 MB |

模型全部按需下载，不勾选的语言不下载。

---

## 附录 A：为什么不用虚拟声卡方案

虚拟声卡（VB-Cable / Voicemeeter）能让"小说软件"单独输出到一条虚拟线路，看上去很直接，但：

- 用户要**手动改每个软件的输出设备**，卸载/换设备时状态残留
- 部分软件（尤其带独占输出的播放器）不认虚拟设备
- 要装内核驱动，杀软误报、系统更新兼容性都是长期维护负担
- 而 WASAPI 进程回环是**系统原生能力**，零驱动、零权限、按 PID 精确隔离

→ 因此虚拟声卡只作为 R1 的**降级备选**，不做主路径。

## 附录 B：为什么主线不用 Windows Live Captions

- 微软**没有公开 API**，只能靠 UI Automation 抓窗口文本 → 系统更新可能随时失效
- 无法拿到时间戳、无法控制断句、无法调节延迟与准确率权衡
- 仅支持部分语言、依赖系统语音包下载

→ 定位为"零安装兜底档"，不是主引擎。

## 附录 C：参考与依据

- [ProcTap — Python 进程级音频采集（MIT，Windows WASAPI 进程回环）](https://github.com/m96-chan/ProcTap)
- [sherpa-onnx（k2-fsa）](https://github.com/k2-fsa/sherpa-onnx) — [DirectML provider 相关 issue](https://github.com/k2-fsa/sherpa-onnx/issues/1240)
- [SenseVoice-ONNX 量化推理实测](https://blog.csdn.net/weixin_26854475/article/details/157500618)
- [Whisper Windows 推理基准：CPU vs CUDA vs Vulkan (2026)](https://starwhisper.ai/whisper-benchmark.html)
- [无虚拟声卡实现 Windows 系统音频实时翻译](https://dev.to/davutakca/translating-windows-system-audio-in-real-time-driverless-with-no-virtual-cable-2842)
- [PySide6 实现覆盖在其他应用之上的窗口（点击穿透与实时跟随）](https://qiita.com/takumi_akashiro/items/59dd9696d3e35e8d863c)

---

*本计划书为 v0.1 立项稿，P1 阶段结束后会根据实测数据修订架构章节。*

### 进度补充（2026-09-12，ASR 模型换代：先查榜单，再用实测说话）

用户找到 HF Open ASR Leaderboard，问要不要换成榜上更好更快的模型；随后指出
「NVIDIA parakeet-tdt-0.6b-ja 是日语最好的」，要求 1~4 全做、除 Whisper turbo 外
其他模型全换新的最优、老模型删掉。

- 先核实榜单覆盖：读其 `constants.py` —— 只评 English + de/fr/it/es/pt/hi/nl，
  **不含中/日/韩/粤**，回答不了我们的问题；榜上第一梯队是 NeMo（Parakeet/Canary），
  而 sherpa-onnx 能跑 NeMo 的 onnx 导出（`from_nemo_ctc`），于是逐个核实并实测。
- 新增 4 个候选并全部下载：`parakeet-ja-int8`（626MB）、`dolphin-base-ctc-int8`（99MB）、
  `omnilingual-300m-ctc-int8`（348MB）、`fire-red-asr2-ctc-zh_en-int8`（740MB）。
  注册表加 `ModelSpec.factory` 字段（模型族→sherpa-onnx 工厂），分块引擎按工厂分发。
- 新增 `scripts/bench_asr_models.py`（同一批音频/同一套引擎代码/同一台机器对拍），
  测试语音从 4 句扩到 11 句（日 5 / 中 4 / 英 2）。
- **实测结论与预期相反**（数据见 `docs/P2-ASR实测.md` 末节）：
  日语 SenseVoice 94.8%（TTS）/ 87.9%（真实录音）> Parakeet-ja 88.1% / 79.7% >
  Dolphin 83.9% / 63.1%；中文流式 97.6% > FireRedASR2 96.2%；英文流式 98.3% ≈ Whisper 97.8%。
  官方 Parakeet 的 CER 是 NeMo **TDT 解码器**的成绩，我们能跑的是 **CTC 导出**，两者不是一回事。
- 因此**默认路由一个没换**（ja/ko/yue 仍 SenseVoice，zh/en 仍流式 zipformer，
  "*" 兜底仍是用户要求保留的 Whisper turbo），4 个新模型作为**可在设置 → 模型里
  一键切换的候选**保留，一个都没删 ——「新」不等于「在我们的音频上更好」。
- 韩语/粤语/小语种本机没有带标注音频（SAPI 只有 zh-CN/en-US/ja-JP），如实标注「未实测」。
- 测试：**357 项**全通过。

### 进度补充（2026-09-12，OpenAI 兼容通道的思考开关）

用户问"访问 OpenAI 兼容接口是不是该默认关思考"，复查后确认：**该关，而且只发一个参数不够**。

- ``LLMConfig.disable_thinking``（默认 True）+ 设置 → 翻译 的「关闭思考（推荐）」勾选框；
- 按服务端类型自动选开关：``chat_template_kwargs.enable_thinking=false``（本地/自建/vLLM）、
  ``reasoning_effort=minimal``（OpenAI 官方）、``reasoning.enabled=false``（OpenRouter）、
  顶层 ``enable_thinking=false``（DashScope）；
- 三层兜底：严格网关 400 → 去掉参数重试一次；空译文+有思考 → ``/no_think`` 软开关重试；
  内联在 ``content`` 里的 ``<think>…</think>``（含被截断的未闭合块）一律剥离，避免思考上屏；
- 设置里的「测试当前翻译通道」改成**真的翻一句**，并报出服务端实际返回的思考字符数
  （新增 ``reasoning_chars_seen`` 计数器）——"关掉了没"以服务端实际行为为准；
- 本机 LM Studio 逐个字段试过（全部 200，未识别的字段会被忽略），确认 payload 安全；
- 新增 ``tests/test_translate_thinking.py``（11 项），全套 **368 项**通过；
  文档见 ``docs/P4-翻译实测.md`` 末节。

### 进度补充（2026-09-13，浏览器标签页音频）

用户问"Edge 一个主程序拖很多标签页，能不能精确到标签页？用扩展还是本体对接？"

**先做判决性实测，再动手**（脚本都在 ``scripts/``，可复现）：

- ``probe_audio_sessions.py`` + ``probe_browser_tabs.py``：同一 Edge 实例开**两个同时出声**
  的标签页，Windows 音频会话仍然只有 **1 个** → **操作系统层没有标签页边界**，
  进程回环与音频会话 API 都到不了这一层。"本体直接对接网页"在物理上不存在；
- ``probe_tab_isolation.py`` + ``probe_tab_mute_extension.py``：把别的标签页静音，
  它们会从进程回环里**彻底消失（−170 dB，数字零）**，目标标签页 **+0.0 dB 分毫不动**、
  可逆可重复 → "扩展只做闸门"这条低成本路线成立，但**有副作用**（别的标签页没声音）；
- ``probe_extension_transport.py``：Edge 153 上扩展连 ``ws://127.0.0.1`` 一次成功
  （101 + 消息到达，Origin 可校验）→ 控制通道不需要 native messaging / 注册表；
- 读 Chromium 源码（``tab_capture_api.cc``）确认唯一门槛：**按标签页授予的
  ``kTabCaptureForTab``**，来源是"用户在该标签页上点扩展图标"。所以产品流程必然是
  "本程序请求 → 用户在浏览器里触发一次"，扩展把这个过程压到**只需点一次**。

**采用实现（用户选择 PCM 直送版）**：

- ``browser_extension/``（MV3，可侧载）：``tabCapture`` 取目标标签页 → offscreen 文档
  **一路接回扬声器**（不做这步用户就听不见）+ 一路 AudioWorklet 出 20ms PCM →
  **自己开 WebSocket 直连本程序**；
- ``app/audio/ws.py``（纯标准库 WebSocket 服务端，**没加任何依赖**）+
  ``app/audio/tab_audio.py``（协议/角色/状态机）+ ``app/audio/ws_client.py``（配套客户端，
  单测/模拟扩展自检共用）；音频复用**同一条** ``AudioPipeline``，识别翻译链路零改动；
- 两条连接按角色管理（SW=control、offscreen=audio），同角色新连接替换旧的（4001）；
- 主窗口加「浏览器标签页」模式 + 「扩展与安装说明…」引导；设置 → 网络 加端口/配对码；
  ``main.py --tab`` 起标签页模式的字幕进程；
- 端到端实测（``probe_tab_capture_e2e.py``，真实 Edge + 真实扩展）：**8/8 通过**——
  服务端收帧 314 / 2.4 MB / 6.3s，标签页通道 441Hz=0.00273 而陪跑 883Hz=0.00000，
  同时整个浏览器混音里 441Hz 与 883Hz **都在**（零副作用 + 回放未间断）；
- 新增 ``tests/test_ws.py``(22) + ``tests/test_tab_audio.py``(28) + ``tests/test_tab_pipeline.py``(13)；
  文档 ``docs/浏览器标签页.md``（原理 / 平台规则 / 协议 / 安装 / 排错 / 6 个坑）。
- 本轮为定位问题顺手做了 ``scripts/cdp_console.py``：CDP 抓扩展各页面（含看不见的
  offscreen 文档）的控制台——扩展跑在浏览器里，它的报错不会出现在我们的日志里。

### 进度补充（2026-09-13，运行中换音源）

用户问："为什么只有程序开始的时候才能选监听的模式和监听程序，进入程序后反而调不了？"

**是历史包袱，不是技术限制。** 三个原因叠加：① 字幕跑在**独立子进程**里
（被 ``app.exec`` 嵌套坑逼的，见 ``launcher.py`` 注释）；② 目标 PID 写死在命令行参数；
③ 控制窗当初只服务于"播放中微调外观"，把音源当成开工前的一次性决定。

改动：

- ``SubtitlePipeline.switch_source(spec, tab_mode)`` + 抽出的 ``restart()``：
  停当前采集 → 重建引擎 → 按新音源开工（与"设置保存后立即生效"共用同一条路）；
  ``reload()`` 现在也走 ``restart()``，行为不变；
- 新控件 ``app/ui/source_picker.py``：列表里同时有**浏览器标签页**与**此刻正在发声的程序**
  （与启动窗口同口径），标出"（当前）"；选中当前音源等于取消（不白重启一次）；
- 控制窗新增「**换音频来源…**」按钮：切完清掉旧字幕并更新目标/状态；
- 顺手修掉相关硬伤：**启动时目标没在发声以前会直接退出**（``return 2``），
  现在先开窗、状态栏显示等待——``CaptureWorker(follow=True)`` 本来就每 2 秒重试解析目标，
  所以"先开字幕窗、再开播放器"现在能自动接上；
- 新增 ``tests/test_source_switch.py``（19 项）；文档补在 ``docs/窗口与退出行为.md`` 第 7 节。
